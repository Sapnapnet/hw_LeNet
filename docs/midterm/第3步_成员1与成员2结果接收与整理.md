# 第3步：成员1与成员2结果接收与整理

完成日期：2026-09-08。成员1和成员2由同一人承担，本次接收其公共网络定义及Float基准部分。总体架构、整组状态和最终冻结信息尚未交付，另待后续处理。

## 1. 做了什么、怎么做

已核查准确率与训练记录、模型源码和网络元数据、5组正式权重、5个参考样本的65组张量及原始MNIST标签。NPY使用禁止pickle的方式读取；model.py仅作文本与AST解析，checkpoint只比对文件哈希，未加载执行。逐项核对张量形状、类型、有限值及TXT/NPY精度，核对样本输入与原始IDX图像、标签的对应关系。

准确率与成员3转交记录相同；checkpoint及网络配置与成员6副本哈希相同，5组weight TXT数值也一致。将成员6原日志预测与本次确认的同一批输入及真实标签关联，补充5个样本类别正确的证据。保留成员6原始误差和参考口径，不修改其历史结果。

已读取run_002的10个NPY及对应PNG，核对1×28×28、float32、0～1及等于PNG/255；结合Baseline的ToTensor代码与参考数据确认输入约定。未重跑预处理，未进行自采识别或HLS定点接入。ROI阈值144及最长边20仍是当前自采策略，没有宣称其已通过模型效果验证。

已由网络层表整理结构说明并生成网络结构图。图中按顺序展示Conv/ReLU/Pool、Flatten、FC及输出后处理，注明形状、无Bias和FC3后无ReLU/Softmax。该图只说明模型，不冒充总体硬件架构。

## 2. 成果文件

路径相对于Project：

- `results/midterm/member1/baseline_summary.md`：Float结果、训练记录、采用清单、与既有结果的关联和限制。
- `results/midterm/member1/network_structure.md`：逐层结构、输入约定和预处理核对表。
- `results/midterm/member1/reference_samples.json`：真实标签、参考预测、成员6日志类别及两套参考的差异。
- `results/midterm/member1/verification.json`：原始文件哈希、5组权重及65组参考张量检查、10张输入格式核对。
- `results/figures/lenet_network_structure.png`：网络结构展示图。
- 本文；同步更新项目README、中期汇总、总方案、自采数据规范及第2步两份文档中的当前状态。

## 3. 验收记录

- [x] 来源及职责明确，179个源文件保持原样，见[核查记录](../results/midterm/member1/verification.json)。未虚构交付版本号或中期冻结版本。
- [x] Float 98.97%及9897/10000与原记录一致，训练10个epoch、最佳第10个已记录，见[结果汇总](../results/midterm/member1/baseline_summary.md)。没有重复计为新实验。
- [x] 正式weight和额外bias文件分开记录，5组正式weight与成员6所用TXT数值一致，原始文件不删除、不修复。
- [x] 5个样本标签和输入可追溯至MNIST IDX，65组张量检查通过，见[样本记录](../results/midterm/member1/reference_samples.json)。
- [x] 成员6样本级真实标签证据已补充，原误差口径不变；最终输出比较没有冒充逐层对齐。
- [x] 当前预处理与本包Float输入约定已核对，10个NPY格式与PNG/255一致，见[输入核对表](../results/midterm/member1/network_structure.md)。未运行模型，批次维度留待接入时增加。
- [x] 网络图、表、图注与来源一致，见[结构图](../results/figures/lenet_network_structure.png)；图像、文件链接及既有成果保留检查已完成。

## 4. 尚未完成的整体事项

成员1/2本次交付范围整理完成，不要求复现。总体架构、整组完成度和中期冻结信息仍待提供；成员7的完整逐层对齐仍待接收。Float→Fixed→HLS验证流程图、整组进度和后续计划、最终材料交接仍按总方案继续。本次未对外发送文件。
